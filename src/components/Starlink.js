import React from "react";
import { Link } from "react-router-dom";

const Starlink = () => {
  return (
    <div>
      Starlink
      <Link to="/">go Home </Link>
    </div>
  );
};

export default Starlink;
